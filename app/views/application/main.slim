p
  = link_to 'Sign in with GitHub', user_omniauth_authorize_path(:github)
- if user_signed_in?
  p
    | Logged in
  p
    = current_user.full_name
  p
  = link_to('View programmers', programmers_path)
  - if current_user.programmer.present?
     p
       = link_to('View your information', programmer_path(current_user.programmer))
  - else
     p
       = link_to('Create programmer account', new_programmer_path)
- else
  p
    | Logged out
